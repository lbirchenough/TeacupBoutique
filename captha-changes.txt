All done. Here's a summary of what was implemented:

Backend (3 services)

TurnstileService.cs registered with AddHttpClient<TurnstileService>() in auth, orders, and payments Program.cs
Turnstile:SecretKey test key added to appsettings.Development.json for all three services
DTOs updated: RegisterRequest + ForgotPasswordRequest (auth), CreateOrderDto (orders), CreatePaymentIntentRequest (payments)
Token verified in AuthController.cs (Register returns 400, ForgotPassword silently returns 200), OrdersController.cs, and the payments create-intent endpoint
Frontend

@marsidev/react-turnstile installed, .env.development created with both test site keys
RegisterRequest + forgotPassword signature updated in api.ts, turnstileToken added to CreateOrderRequest in types.ts, createIntent updated in paymentsApi.ts
register.tsx + forgot-password.tsx: managed checkbox widget added, submit button gated on token
cart.tsx: invisible widget auto-runs on render, Place Order button gated on token, falls back to managed checkbox if invisible fails
StripePaymentForm.tsx: invisible widget runs first, createIntent query only fires once token is obtained, fallback managed checkbox shown on error
When you set up your real Cloudflare account, swap out the test keys in .env.development and the three appsettings.Development.json files with your real keys.